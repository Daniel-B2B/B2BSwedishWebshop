@model CheckoutCompletedModel
@using Nop.Web.Models.Checkout;
@{
    Layout = "~/Views/Shared/_ColumnsOne.cshtml";

    //title
    Html.AddTitleParts(T("PageTitle.Checkout").Text);
    //page class
    Html.AppendPageCssClassParts("html-checkout-page");
    Html.AppendPageCssClassParts("html-order-completed-page");
}
<div class="page checkout-page order-completed-page">
    @*@if (!Model.OnePageCheckoutEnabled)
    {
        @Html.Action("CheckoutProgress", "Checkout", new { step = CheckoutProgressStep.Complete })
    }*@
    <div class="page-title">
        <h1>@T("Checkout.ThankYou")</h1>
    </div>
	  <p class="subscribtion text-center">Du har modtaget en ordrebekræftelse. Herunder kan du se de næste trin:</p>
    <div>
        <ul class="order-complete-list">
            <li>
                <h3>Trin 1</h3>
                <h3>Ordrebekræftelse og digital trykprøve</h3>
                <p>Du modtager inden for 48 timer en endelig ordrebekræftelse sammen med en grafisk digital trykprøve</p>

            </li>
            <li>
                <h3>Trin 2</h3>
                <h3>Alt som forventet?</h3>
                <p>Du bedes bekræfte at alt er som du forventer ved at besvare pr. e-mail hvorefter dine varer sættes i produktion</p>
            </li>
            <li>
                <h3>Trin 3</h3>
                <h3>Levering og betaling</h3>
                <p>
                    Du modtager typisk dine varer inden for 14 hverdage på din ønskede leveringsadresse Såfremt du har ønsket online-betaling trækkes beløbet når varer afsendes fra lager
                </p>
            </li>
        </ul>

        <div class="buttons">
            <input type="button" value="@T("Checkout.ThankYou.Continue")" class="button-1 order-completed-continue-button" onclick="setLocation('@Url.RouteUrl("HomePage")')" />
        </div>

    </div>
    @*<div class="page-body checkout-data">
        @Html.Widget("checkout_completed_top", Model.OrderId)
        <div class="section order-completed">
            <div class="title">
                <strong>@T("Checkout.YourOrderHasBeenSuccessfullyProcessed")</strong>
            </div>
            <div class="details">
                <div class="order-number">
                    <strong>@T("Checkout.OrderNumber"): @Model.CustomOrderNumber</strong>
                </div>
                <div class="details-link">
                    <a href="@Url.RouteUrl("OrderDetails", new { orderID = Model.OrderId })">@T("Checkout.PlacedOrderDetails")</a>
                </div>
            </div>
            <div class="buttons">
                <input type="button" value="@T("Checkout.ThankYou.Continue")" class="button-1 order-completed-continue-button" onclick="setLocation('@Url.RouteUrl("HomePage")')" />
            </div>
        </div>
        @Html.Widget("checkout_completed_bottom", Model.OrderId)
    </div>
</div>*@
